//CSS3 pie 可以让IE 6 7 8 9支持部分圆角，阴影，渐变背景功能,修复IE6 PNG24透明度。
/*(function() {
	Modernizr.load({
  test: Modernizr.borderradius && Modernizr.boxshadow && Modernizr.cssgradients,
  nope: 'assets/polyfills/pie/pie.1.0.js',
  callback: function () {
    if (window.PIE) {
        $('.fix_ie_css3').each(function() {
            PIE.attach(this);
        });
    }
  }
});
}());
*/


